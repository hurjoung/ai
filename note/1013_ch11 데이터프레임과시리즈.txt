판타스 패키지
- import pandas

- 딕셔너리 리스트를 이용해서 데이터프레임 만들기
    .to_dict() # 데이터프레임을 딕셔너리로
    .dtypes # 열의 타입
    .info()
- 리스트를 이용해서 데이터프레임 만들기

- read_csv

- 열이름 지정
  .columns

- 행이름 지정
   .index

- 부분 데이터 조회
   ['열이름']
   .열이름

- loc을 이용한 조회
   .loc[행이름, 열이름] : 행이름, 열이름 자리에 슬라이싱 from:to => from부터 to까지(to포함)

- iloc을 이용한 조회
   .iloc[행순번, 열순번] : 행순번, 열순번 자리에 슬라이싱 from:to:by => from부터 to앞까지 by식(to미포함)

- 조건으로 조회
  df[조건] : 조건에 맞는 모든 행 조회.

- 결측치 확인
   .isna() # 결측치 여부
   .isna().sum(axis=0) # 각 열의 결측치 갯수